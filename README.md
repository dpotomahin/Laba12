# Java Collections Lab (Lab 12)

This project demonstrates the usage of `Set` (HashSet) and `Map` (HashMap) in Java.
It manages a collection of Projector objects.

## Features
- Stores unique projectors using `HashSet`.
- Groups projectors by brand using `Map`.
- Counts projectors by resolution using `Map`.

## Author
Variant 11 Student
